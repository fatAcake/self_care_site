# Инструкция по деплою на Render

## 📋 Что нужно сделать

### 1. Подготовка проекта

1. **Убедитесь, что все изменения закоммичены в Git**
2. **Подключите репозиторий к Render** (через GitHub/GitLab/Bitbucket)

---

## 🗄️ Шаг 1: Создание PostgreSQL базы данных

1. Зайдите на [render.com](https://render.com) и войдите в аккаунт
2. Нажмите **"New +"** → **"PostgreSQL"**
3. Настройки:
   - **Name**: `self-care-db`
   - **Database**: `self_careDB`
   - **User**: `selfcare_user`
   - **Plan**: Starter (бесплатный)
4. Нажмите **"Create Database"**
5. **Скопируйте Internal Database URL** (он понадобится позже)

---

## 🔧 Шаг 2: Создание Backend API

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш Git репозиторий
3. Настройки:
   - **Name**: `self-care-api`
   - **Environment**: `Dotnet`
   - **Region**: Выберите ближайший
   - **Branch**: `main` (или ваша основная ветка)
   - **Root Directory**: Оставьте пустым (или `Self_care` если backend в подпапке)
   - **Build Command**: 
     ```bash
     dotnet restore && dotnet publish -c Release -o ./publish
     ```
   - **Start Command**: 
     ```bash
     cd publish && dotnet Self_care.dll
     ```
   - **Plan**: Starter (бесплатный)

4. **Environment Variables** (добавьте в разделе "Environment"):
   ```
   ASPNETCORE_ENVIRONMENT=Production
   ConnectionStrings__DefaultConnection=Host=ВАШ_ХОСТ;Database=self_careDB;Username=ВАШ_ПОЛЬЗОВАТЕЛЬ;Password=ВАШ_ПАРОЛЬ;SSL Mode=Require
   Jwt__Key=ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_МИНИМУМ_32_СИМВОЛА_ДЛЯ_БЕЗОПАСНОСТИ
   Jwt__Issuer=Self_care
   Jwt__Audience=SelfCare.Front
   FRONTEND_URL=https://your-frontend-name.onrender.com
   ```
   
   ⚠️ **Важно**: 
   - Замените `ConnectionStrings__DefaultConnection` на Internal Database URL из шага 1
   - Сгенерируйте сильный JWT ключ (минимум 32 символа)
   - `FRONTEND_URL` пока оставьте placeholder, обновите после создания фронтенда

5. Нажмите **"Create Web Service"**

6. **Дождитесь первого деплоя** и скопируйте URL вашего API (например: `https://self-care-api.onrender.com`)

---

## 🎨 Шаг 3: Создание Frontend

1. Нажмите **"New +"** → **"Static Site"**
2. Подключите ваш Git репозиторий
3. Настройки:
   - **Name**: `self-care-frontend`
   - **Branch**: `main` (или ваша основная ветка)
   - **Root Directory**: `self_care_front`
   - **Build Command**: 
     ```bash
     npm install && npm run build
     ```
   - **Publish Directory**: `dist`
   - **Plan**: Starter (бесплатный)

4. **Environment Variables** (добавьте в разделе "Environment"):
   ```
   VITE_API_BASE_URL=https://self-care-api.onrender.com/api
   ```
   
   ⚠️ Замените на реальный URL вашего API из шага 2

5. Нажмите **"Create Static Site"**

---

## 🔄 Шаг 4: Обновление CORS в Backend

После создания фронтенда:

1. Вернитесь в настройки вашего **Backend API**
2. Обновите переменную окружения:
   ```
   FRONTEND_URL=https://self-care-frontend.onrender.com
   ```
   (замените на реальный URL вашего фронтенда)
3. Нажмите **"Save Changes"** - это запустит пересборку

---

## 🗄️ Шаг 5: Миграции базы данных

После деплоя backend нужно выполнить миграции:

### Вариант 1: Через Render Shell
1. В настройках Backend API нажмите **"Shell"**
2. Выполните команды:
   ```bash
   cd /opt/render/project/src
   dotnet ef migrations add InitialCreate
   dotnet ef database update
   ```

### Вариант 2: Локально (если настроен доступ)
```bash
cd Self_care
dotnet ef migrations add InitialCreate
dotnet ef database update
```

---

## 📝 Переменные окружения - полный список

### Backend (self-care-api):
```
ASPNETCORE_ENVIRONMENT=Production
ConnectionStrings__DefaultConnection=Host=...;Database=self_careDB;Username=...;Password=...;SSL Mode=Require
Jwt__Key=ваш_секретный_ключ_минимум_32_символа
Jwt__Issuer=Self_care
Jwt__Audience=SelfCare.Front
FRONTEND_URL=https://self-care-frontend.onrender.com
```

### Frontend (self-care-frontend):
```
VITE_API_BASE_URL=https://self-care-api.onrender.com/api
```

---

## ⚙️ Дополнительные настройки

### Auto-Deploy
- По умолчанию включено автоматическое обновление при push в Git
- Можно отключить в настройках сервиса

### Custom Domains
- В настройках каждого сервиса можно добавить свой домен
- Render автоматически настроит SSL сертификат

### Health Checks
- Backend: Render автоматически проверяет доступность
- Можно настроить custom health check endpoint

---

## 🐛 Решение проблем

### Backend не запускается
1. Проверьте логи в разделе "Logs"
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных доступна

### Frontend не подключается к API
1. Проверьте `VITE_API_BASE_URL` в переменных окружения
2. Убедитесь, что CORS настроен правильно в backend
3. Проверьте, что API доступен (откройте URL в браузере)

### Ошибки миграций
1. Убедитесь, что connection string правильный
2. Проверьте, что база данных создана
3. Убедитесь, что у пользователя есть права на создание таблиц

---

## 📊 Мониторинг

- **Logs**: Доступны в реальном времени в панели каждого сервиса
- **Metrics**: CPU, Memory, Network в разделе "Metrics"
- **Events**: История деплоев и изменений

---

## 💰 Стоимость

- **Starter Plan**: Бесплатный (с ограничениями)
  - Backend: может "засыпать" после 15 минут неактивности
  - Frontend: всегда доступен
  - Database: до 90 дней хранения

- **Paid Plans**: От $7/месяц за сервис
  - Backend всегда активен
  - Больше ресурсов
  - Приоритетная поддержка

---

## 🔐 Безопасность

1. **Никогда не коммитьте** `.env` файлы в Git
2. Используйте **сильные пароли** для базы данных
3. Генерируйте **уникальный JWT ключ** (минимум 32 символа)
4. Регулярно **обновляйте зависимости**

---

## ✅ Чек-лист перед деплоем

- [ ] PostgreSQL база данных создана
- [ ] Backend API создан и задеплоен
- [ ] Все переменные окружения установлены
- [ ] Frontend создан с правильным API URL
- [ ] CORS обновлен с URL фронтенда
- [ ] Миграции базы данных выполнены
- [ ] Протестирована регистрация/авторизация
- [ ] Проверена работа всех функций

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи в панели Render
2. Убедитесь, что все переменные окружения установлены
3. Проверьте документацию Render: https://render.com/docs
4. Обратитесь в поддержку Render через панель управления

